import { Box } from '@mui/material';
import styled from 'styled-components';

export const Container = styled(Box)`
   display: flex;
   height: 100vh;
   alignItems: 'center';
   background: #F9F9FA;
`;
